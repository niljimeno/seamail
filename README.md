# Seamail

Minimal email server written in golang using emersion libs.

Supports SMTP and a custom simplified JSON api, to make it easier to interact with.

## Status

Barely functional, not polished, usage not recommended.
It doesn't even have a spam filter yet.

Current working apps:

- `cmd/server` - server implementation
- `cmd/cursed` - TUI client
